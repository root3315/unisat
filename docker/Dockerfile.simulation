FROM python:3.11-slim

WORKDIR /app

COPY simulation/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY simulation/ .
COPY mission_config.json .

RUN mkdir -p output

CMD ["python", "mission_analyzer.py"]
